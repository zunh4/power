// 클릭 가능한 영역 요소 가져오기
const clickableArea = document.getElementById('target');

// 클릭 이벤트 추가
clickableArea.addEventListener('click', () => {
    // 페이지 이동
    window.location.href = 'index5.html';
});
